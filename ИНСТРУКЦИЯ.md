# Инструкция по использованию системы классификации отходов

## Как это работает?

### 1. Архитектура системы

Система состоит из нескольких компонентов:

**data_loader.py** - Модуль загрузки данных
- Использует OpenCV для загрузки изображений
- Применяет аугментацию данных (повороты, отражения, изменение яркости) для улучшения обучения
- Разделяет данные на обучающую (70%), валидационную (10%) и тестовую (20%) выборки
- Нормализует изображения для предобученной модели

**model.py** - Архитектура нейронной сети
- Использует предобученную ResNet18 (обучена на ImageNet)
- Transfer Learning: замораживает начальные слои, обучает только последние
- Добавляет dropout для предотвращения переобучения
- Выходной слой классифицирует на 5 классов

**train.py** - Обучение модели
- Обучает модель на обучающей выборке
- Проверяет качество на валидационной выборке
- Сохраняет лучшую модель (по валидационной точности)
- Создает графики процесса обучения

**evaluate.py** - Оценка модели
- Тестирует модель на тестовой выборке
- Создает confusion matrix (матрицу ошибок)
- Вычисляет точность по каждому классу
- Генерирует визуализации результатов

**predict.py** - Предсказания
- Загружает изображение через OpenCV
- Предобрабатывает его
- Делает предсказание с вероятностями
- Визуализирует результат

### 2. Пошаговая инструкция

#### Шаг 1: Установка зависимостей

```bash
pip install -r requirements.txt
```

#### Шаг 2: Проверка датасета

Убедитесь, что ваши изображения организованы так:
```
garbage-dataset/
├── plastic/
│   ├── image1.jpg
│   ├── image2.jpg
│   └── ...
├── glass/
├── paper/
├── metal/
└── biological/
```

#### Шаг 3: Обучение модели

```bash
python train.py
```

Или с параметрами:
```bash
python train.py --epochs 30 --lr 0.0001 --batch_size 16
```

**Что происходит:**
- Загружаются все изображения из датасета
- Данные разделяются на train/val/test
- Модель обучается эпоха за эпохой
- На каждой эпохе вычисляется loss и accuracy
- Сохраняется лучшая модель в `models/best_model.pth`
- Создается график обучения в `results/training_history.png`

**Время обучения:** зависит от размера датасета и GPU. На CPU может занять несколько часов.

#### Шаг 4: Оценка модели

```bash
python evaluate.py
```

**Что происходит:**
- Загружается сохраненная модель
- Тестируется на тестовой выборке
- Создаются визуализации:
  - Confusion matrix (какие классы путаются)
  - Точность по классам (какой класс распознается лучше)
  - Распределение классов (сколько примеров каждого класса)

#### Шаг 5: Предсказания на новых изображениях

```bash
python predict.py --image path/to/your/image.jpg
```

Или для нескольких изображений:
```bash
python predict.py --image_dir path/to/images/ --num_images 5
```

**Что происходит:**
- Загружается изображение через OpenCV
- Применяется предобработка (resize, нормализация)
- Модель делает предсказание
- Показываются вероятности для всех классов
- Результат сохраняется в `results/predictions/`

### 3. Понимание результатов

#### Confusion Matrix (Матрица ошибок)
- Показывает, сколько примеров каждого класса было правильно/неправильно классифицировано
- Диагональ = правильные предсказания
- Вне диагонали = ошибки классификации

#### График обучения
- **Loss (потери)**: должна уменьшаться со временем
- **Accuracy (точность)**: должна увеличиваться
- Если validation accuracy перестает расти, возможно переобучение

#### Точность по классам
- Показывает, какой класс распознается лучше/хуже
- Если один класс имеет низкую точность, возможно:
  - Недостаточно примеров этого класса
  - Класс похож на другой (например, пластик и стекло)

### 4. Улучшение результатов

Если точность низкая:

1. **Больше данных**: добавьте больше изображений (минимум 200 на класс)
2. **Аугментация**: увеличьте аугментацию в `data_loader.py`
3. **Больше эпох**: обучите дольше (`--epochs 50`)
4. **Learning rate**: попробуйте разные значения (`--lr 0.0001` или `--lr 0.01`)
5. **Другая архитектура**: попробуйте ResNet50 вместо ResNet18 в `model.py`

### 5. Технические детали

**Transfer Learning:**
- Используем предобученную ResNet18 (обучена на ImageNet)
- Замораживаем начальные слои (извлекают общие признаки)
- Обучаем только последние слои (специфичные для нашей задачи)

**Аугментация данных:**
- Горизонтальное отражение (50% вероятность)
- Поворот на ±15 градусов
- Изменение яркости и контраста
- Помогает модели лучше обобщаться

**Нормализация:**
- Используем ImageNet статистику (mean=[0.485, 0.456, 0.406])
- Это нужно для предобученной модели

### 6. Примеры использования

**Быстрый старт:**
```bash
# 1. Установка
pip install -r requirements.txt

# 2. Обучение (20 эпох)
python train.py

# 3. Оценка
python evaluate.py

# 4. Предсказание
python predict.py --image garbage-dataset/plastic/plastic_1.jpg
```

**Продвинутое использование:**
```bash
# Обучение с настройками
python train.py --epochs 50 --lr 0.0001 --batch_size 16

# Оценка конкретной модели
python evaluate.py --model_path models/best_model.pth

# Предсказание для директории
python predict.py --image_dir test_images/ --num_images 20
```

### 7. Структура результатов

После обучения и оценки у вас будет:

```
results/
├── training_history.png          # График обучения
├── confusion_matrix.png          # Матрица ошибок
├── confusion_matrix_normalized.png  # Нормализованная матрица
├── class_accuracy.png            # Точность по классам
├── class_distribution.png        # Распределение классов
└── predictions/                  # Результаты предсказаний
    └── image_name.jpg
```

### 8. Требования к изображениям

- Форматы: JPG, JPEG, PNG
- Размер: любой (автоматически изменяется до 224x224)
- Качество: чем лучше качество, тем лучше результаты
- Разнообразие: изображения из разных условий (освещение, фон, угол)

### 9. Устранение проблем

**Ошибка: "CUDA out of memory"**
- Уменьшите batch_size: `--batch_size 16` или `--batch_size 8`

**Низкая точность:**
- Проверьте баланс классов (должно быть примерно одинаково)
- Добавьте больше данных
- Увеличьте количество эпох

**Модель не сохраняется:**
- Убедитесь, что есть права на запись
- Проверьте наличие директории `models/`

---

**Удачи с проектом! 🚀**

